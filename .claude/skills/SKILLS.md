# Build-Depot Skills

Repo-local skills for the Build-Depot software factory:

- `build-depot-quality` — quality checks, graph alignment, and quality review.
- `build-depot-security` — dependency audit, secret scan, and webhook security.
- `build-depot-delivery` — preflight, CI/CD, deploy, smoke, and rollback.

Tool-specific skill directories should point here:

- `.codex/skills -> ../.claude/skills`
- `.cursor/skills -> ../.claude/skills`
