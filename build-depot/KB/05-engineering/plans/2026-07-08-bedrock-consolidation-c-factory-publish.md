# Plan C — Lightweight factory integration + CI + Shipyard publish + doc charter

> Architect analysis 2026-07-08 (build-depot read at source; bedrock-consolidated 4be1e855). Full path:line analysis in SDD transcript a4015406.

## Honesty header
build-depot integration is a PULL/SCAN: factory-adoption-doctor walks ~/dev/reflective/*, reads AGENTS.md/Justfile/workflows, grades a fixed contract (agents/just-ci/ci-workflow/security/delivery/linear-label/sentry/local-policy signals). Bedrock "registers" by EXPOSING evidence, not calling APIs. bedrock-consolidated is already in factory-cohorts.json as Tier-1 "future consolidated adoption target" (doctor BLOCKS on missing signals incl. delivery) but absent from REPO_CONTEXT. PR-gate is a Trigger.dev webhook task (not wired); arena→Omnigraph ingest DOES NOT EXIST (ingest deferred; debt-tracker normalizes GitHub/Linear/Sentry only). Depot-side work = RFL-174 backlog, out of scope.
LOAD-BEARING BUG: arena-driver locate_workspace_root wants MASTERPLAN.md+KB/ (absent in consolidated repo; kb/ lowercase) and writes to arena-tests/reports (nonexistent) — arena bails immediately in the consolidated clone. Same family as RFL-177; fix is Plan C task A1, shared prerequisite with RFL-155's dimension work.

## Decisions
C1 publish tool: cargo-workspaces publish --registry reflective-labs (P1 spike verifies non-crates.io registry forwarding; fallback = root-Justfile topo loop). C2 published set = cargo metadata publish flags (not lists). C3 tag v4.* on consolidation/main triggers publish.yml; token/SSH via GitHub secrets only. C4 name reservation: bedrock reserves helm-*/all names on reflective-labs; app-platform must never republish spine crates; no collision with helms 0.3.0/0.1.0 (different index). C5 just ci = fmt-check+check+lint+test; security-audit SEPARATE (RP-CI-PARITY: advisory verdicts change with the world). C6 PR = default-members+changed; main/nightly = full workspace+solvers+arena+soak. C7 native solvers = dedicated cached job (sizing SPIKE — free runners may not fit OR-Tools/cvc5). C8 arena gate after A1 (exit-code policy from driver). C9 depot = evidence-surface only (Tier-1 doctor pass); RFL-174 for depot-side. C10 doc charter: root=entry points (README rewrite, EPIC/MILESTONES deleted, STRATEGY+docs/strategy leave for root-repo KB after verify, docs/ eliminated); kb/ canonical. C11 one root .claude/skills (main-only trunk); stale per-subsystem skills deleted (converge's says "main + next").

## Streams & tasks
P (publish): P1 tool spike → P2 publish.yml (tag-triggered, dry-run-first) → P3 publish-dry PR gate (Cargo.toml-touching PRs) → P4 reservation/collision note.
C (CI): C-1 honest Justfile (capital J) → C-2 ci.yml PR (just ci, foundation+changed, sccache/solver cache) ∥ C-3 ci-full.yml main+nightly (workspace, solvers, arena after A1, nightly soak) ∥ C-4 security.yml scheduled.
A (arena+depot evidence): A1 arena-driver root/report-path fix (ONLY crate-source task; coordinate with RFL-155 arena work = RFL-177) → A2 adoption-contract compliance (AGENTS wording, module:bedrock-consolidated label, sentry not-applicable declaration, delivery recipe; gate = factory-adoption-doctor shows ADOPTED Tier1) → A3 RFL-174 depot backlog annotation (flag only).
D (docs): D1 root markdown triage (verify-then-move strategy content) ∥ D2 AGENTS.md single-repo rewrite ∥ D3 skills reconciliation ∥ D4 kb index + self-containment audit (flag gaps, don't silently backfill) ∥ D5 cutover-enablement (registry consumption guide for engagements; runway migration note incl. absent provider-api/domain mapping).
Sequencing: {P1,A1,D1,D2} → C-1 → {C-2,C-3,C-4,P2,P3,A2} → {D3,D4,D5} anytime. Cutover-ready = P2 dry-run green + A2 Tier-1 + C-3 arena green + D5 published.

## Risks
(1) native-solver CI cost — spike, larger runner/prebuilt cache. (2) arena dead until A1. (3) cargo-workspaces registry support — P1 de-risks. (4) "CI calls depot" expectation reset — evidence surface, not invocation. (5) Tier-1 blocking grade needs the delivery signal (publish flow provides). (6) REPO_CONTEXT gap until depot adds entry. (7) strategy-content deletion verify-first. (8) stale skills misdirecting agents until D3.
