# Vendored: forge-templates

Snapshot of `Reflective-Lab/forge-templates` (commit `e327134`), vendored into
machinery on 2026-07-23 as part of the root-dissolution restructure (templates are
build scaffolding → they live with the build factory).

- **Canonical home:** here, `machinery/forge-templates/`. Edit here.
- **History:** the standalone `forge-templates` repo retains full commit history and
  is parked at `~/dev/forge-templates` (out of the reflective tree). This copy is a
  snapshot — no `.git`.
